retrieving ...
checking platform for `readline' ...
checking dependencies for `readline' ...
install order:
("readline")
installing readline: ...
changing current directory to .
  '/usr/bin/csi' -bnq -setup-mode -e "(require-library setup-api)" -e "(import setup-api)" -e "(setup-error-handling)" -e "(extension-name-and-version '(\"readline\" \"\"))" 'readline.setup'
rm -f readline-test
rm -f readline.c
trying: -lreadline -lhistory -ltermcap
  '/usr/bin/csc' -feature compiling-extension -setup-mode    readline-test.scm -C -D_GNU_SOURCE=1 -C -std=gnu99 -lreadline -lhistory -ltermcap
/usr/bin/ld: cannot find -ltermcap
collect2: error: ld returned 1 exit status

Error: shell command terminated with non-zero exit status 256: 'gcc' 'readline-test.o' -o 'readline-test' -lreadline -lhistory -ltermcap -L"/usr/lib"  -Wl,-R"/usr/lib" -lchicken -lm -ldl
trying: -lreadline -lhistory -lcurses
  '/usr/bin/csc' -feature compiling-extension -setup-mode    readline-test.scm -C -D_GNU_SOURCE=1 -C -std=gnu99 -lreadline -lhistory -lcurses
  '/usr/bin/csc' -feature compiling-extension -setup-mode    -s -O2 readline.scm -C -D_GNU_SOURCE=1 -C -std=gnu99 -lreadline -lhistory -lcurses
  '/usr/bin/csc' -feature compiling-extension -setup-mode    -c -O2 -d0 -j readline readline.scm -o readline-static.o -unit readline -C -D_GNU_SOURCE=1 -C -std=gnu99 -lreadline -lhistory -lcurses
  '/usr/bin/csc' -feature compiling-extension -setup-mode    -s -O2 -d0 readline.import.scm
  cp -r 'readline.so' '/usr/lib/chicken/7/readline.so'
  chmod a+r '/usr/lib/chicken/7/readline.so'
  cp -r 'readline.import.so' '/usr/lib/chicken/7/readline.import.so'
  chmod a+r '/usr/lib/chicken/7/readline.import.so'
  cp -r 'readline-static.o' '/usr/lib/chicken/7/readline-static.o'
  chmod a+r '/usr/lib/chicken/7/readline-static.o'
  chmod a+r '/usr/lib/chicken/7/readline.setup-info'
